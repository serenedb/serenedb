---
title: Overview
sidebar_position: 1
---

import SqlLogicTest from "@site/src/components/SqlLogicTest";
import DocCallout from "@site/src/components/DocCallout";

## Examples

Read a single Parquet file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_001" />

Figure out which columns/types are in a Parquet file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_002" />

Create a table from a Parquet file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_003" />

If the file does not end in `.parquet`, use the `read_parquet` function:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_004" />

Use list parameter to read three Parquet files and treat them as a single table:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_005" />

Read all files that match the glob pattern:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_006" />

Read all files that match the glob pattern, and include the `filename` virtual column that specifies which file each row came from:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_007" />

Use a list of globs to read all Parquet files from two specific folders:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_008" />

Read over HTTPS:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_009" />

Query the [metadata of a Parquet file](./metadata.md#parquet-metadata):

<SqlLogicTest id="data_import_and_export/parquet/overview/example_010" />

Query the [file metadata of a Parquet file](./metadata.md#parquet-file-metadata):

<SqlLogicTest id="data_import_and_export/parquet/overview/example_011" />

Query the [key-value metadata of a Parquet file](./metadata.md#parquet-key-value-metadata):

<SqlLogicTest id="data_import_and_export/parquet/overview/example_012" />

Query the [schema of a Parquet file](./metadata.md#parquet-schema):

<SqlLogicTest id="data_import_and_export/parquet/overview/example_013" />

Write the results of a query to a Parquet file using the default compression (Snappy):

<SqlLogicTest id="data_import_and_export/parquet/overview/example_014" />

Write the results from a query to a Parquet file with specific compression and row group size:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_015" />

Export the table contents of the entire database as parquet:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_016" />

## Parquet Files

Parquet files are compressed columnar files that are efficient to load and process. SereneDB provides support for both reading and writing Parquet files in an efficient manner, as well as support for pushing filters and projections into the Parquet file scans.

<DocCallout type="tip">
Parquet datasets differ based on the number of files, the size of individual files, the compression algorithm used, row group size, etc. These have a significant effect on performance. Please consult the [Performance Guide](../../cookbook/performance/file_formats.md) for details.
</DocCallout>

## `read_parquet` Function

| Function                              | Description              | Example                                       |
| :------------------------------------ | :----------------------- | :-------------------------------------------- |
| `read_parquet(path_or_list_of_paths)` | Read Parquet file(s)     | `SELECT * FROM read_parquet('test.parquet');` |
| `parquet_scan(path_or_list_of_paths)` | Alias for `read_parquet` | `SELECT * FROM parquet_scan('test.parquet');` |

If your file ends in `.parquet`, the function syntax is optional. The system will automatically infer that you are reading a Parquet file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_017" />

Multiple files can be read at once by providing a glob or a list of files. Refer to the [multiple files section](../multiple_files/overview.md) for more information.

### Parameters

There are a number of options exposed that can be passed to the `read_parquet` function or the [`COPY` statement](../../sql/statements/copy/index.md).

| Name                | Description                                                                                                                                                                                            | Type     | Default         |
| :------------------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------- | :-------------- |
| `binary_as_string`  | Parquet files generated by legacy writers do not correctly set the `UTF8` flag for strings, causing string columns to be loaded as `BLOB` instead. Set this to true to load binary columns as strings. | `BOOL`   | `false`         |
| `encryption_config` | Configuration for [Parquet encryption](../parquet/encryption.md).                                                                                                                                      | `STRUCT` | -               |
| `filename`          | Whether or not an extra `filename` column should be included in the result. The `filename` column is added automatically as a virtual column and this option is only kept for compatibility reasons.   | `BOOL`   | `false`         |
| `file_row_number`   | Whether or not to include the `file_row_number` column.                                                                                                                                                | `BOOL`   | `false`         |
| `hive_partitioning` | Whether or not to interpret the path as a [Hive partitioned path](../partitioning/hive_partitioning.md).                                                                                               | `BOOL`   | (auto-detected) |
| `union_by_name`     | Whether the columns of multiple schemas should be [unified by name](../multiple_files/combining_schemas.md), rather than by position.                                                                  | `BOOL`   | `false`         |
| `schema`            | Allows you to read a Parquet file as if it has the supplied schema. Field IDs are required.                                                                                                            | `MAP`    | `NULL`          |

## Using the `schema` Parameter

The `schema` parameter allows you to read the Parquet file using a specific schema. This is useful for renaming, adding, deleting, reordering, or casting columns when reading Parquet files.

To use the `schema` parameter, field IDs are required. To make them available when creating the Parquet using SereneDB, use:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_018" />

Reading Parquet files:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_019" />

<DocCallout type="tip">
The `schema` parameter cannot be combined with `union_by_name = true`.
</DocCallout>

## Partial Reading

SereneDB supports projection pushdown into the Parquet file itself. That is to say, when querying a Parquet file, only the columns required for the query are read. This allows you to read only the part of the Parquet file that you are interested in. This will be done automatically by SereneDB.

SereneDB also supports filter pushdown into the Parquet reader. When you apply a filter to a column that is scanned from a Parquet file, the filter will be pushed down into the scan, and can even be used to skip parts of the file using the built-in [zonemaps](../../cookbook/performance/indexing.md#zonemaps) (also known as [min-max indexes](https://en.wikipedia.org/wiki/Block_Range_Index)). Note that this will depend on whether or not your Parquet file contains zonemaps.

Filter and projection pushdown provide significant performance benefits.

## Inserts and Views

You can also insert the data into a table or create a table from the Parquet file directly. This will load the data from the Parquet file and insert it into the database:

Insert the data from the Parquet file in the table:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_020" />

Create a table directly from a Parquet file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_021" />

If you wish to keep the data stored inside the Parquet file, but want to query the Parquet file directly, you can create a view over the `read_parquet` function. You can then query the Parquet file as if it were a built-in table:

Create a view over the Parquet file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_022" />

Query the Parquet file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_023" />

## Writing to Parquet Files

SereneDB also has support for writing to Parquet files using the `COPY` statement syntax. See the [`COPY` Statement page](../../sql/statements/copy/index.md) for details, including all possible parameters for the `COPY` statement.

Write a query to a Snappy-compressed Parquet file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_024" />

Write `tbl` to a zstd-compressed Parquet file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_025" />

Write `tbl` to a zstd-compressed Parquet file with the lowest compression level yielding the fastest compression:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_026" />

Write to Parquet file with [key-value metadata](../parquet/metadata.md#parquet-key-value-metadata):

<SqlLogicTest id="data_import_and_export/parquet/overview/example_027" />

Write to a Parquet v2 file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_028" />

Write a CSV file to an uncompressed Parquet file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_029" />

Write a query to a Parquet file with zstd-compression and row group size:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_030" />

Write data to an LZ4-compressed Parquet file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_031" />

Or, equivalently:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_032" />

Write data to a Brotli-compressed Parquet file:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_033" />

To configure the page size of Parquet file's dictionary pages, use the `STRING_DICTIONARY_PAGE_SIZE_LIMIT` option (default: 1 MB):

<SqlLogicTest id="data_import_and_export/parquet/overview/example_034" />

SereneDB's `EXPORT` command can be used to export an entire database to a series of Parquet files. See the [“`EXPORT` statement” page](../../sql/statements/export_and_import_database/index.md) for more details:

Export the table contents of the entire database as Parquet:

<SqlLogicTest id="data_import_and_export/parquet/overview/example_035" />

## Encryption

SereneDB supports reading and writing [encrypted Parquet files](./encryption.md).

## Supported Features

The list of supported Parquet features is available in the [Parquet documentation's “Implementation status” page](https://parquet.apache.org/docs/file-format/implementationstatus/).
