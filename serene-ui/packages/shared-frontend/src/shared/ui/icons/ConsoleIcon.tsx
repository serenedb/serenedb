import { TerminalSquareIcon } from "lucide-react";

export const ConsoleIcon = (props: React.SVGProps<SVGSVGElement>) => (
    <TerminalSquareIcon
        style={{ width: 18 }}
        preserveAspectRatio="none"
        {...props}
    />
);
