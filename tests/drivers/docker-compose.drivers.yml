services:
  serenedb-single:
    image: ${BUILD_IMAGE}
    user: "${DOCKER_UID:-1000}:${DOCKER_GID:-1000}"
    security_opt:
      - seccomp:unconfined
    volumes:
      - "${WORKSPACE}:/serenedb"
      - "${WORKSPACE}/out/logs:/var/log/serenedb"
    env_file:
      - "${WORKSPACE}/docker.env"
    environment:
      - BUILD_DIR
      - HOME=/serenedb
    networks:
      - test-network
    command: >
      sh -c '
        exec /serenedb/${BUILD_DIR}/bin/serened /tmp/serenedb_datadir \
          --server_endpoint pgsql+tcp://0.0.0.0:5432 \
      '

  tests:
    image: ${BUILD_IMAGE}
    user: "${DOCKER_UID:-1000}:${DOCKER_GID:-1000}"
    group_add:
      - "${DOCKER_SOCK_GID:-999}"
    volumes:
      - "${WORKSPACE}:/serenedb"
      - "${WORKSPACE}/out/drivers-tests:/out"
      - "${WORKSPACE}/out/logs:/logs"
    depends_on:
      serenedb-single:
        condition: service_started
    env_file:
      - "${WORKSPACE}/docker.env"
    environment:
      - HOME=/tmp/home
      - LOG_DIR=/logs
      - SDB_DRV_LANG
      - SDB_FUZZ_QUERIES
      - SDB_FUZZ_PARALLEL
    networks:
      - test-network
    command: >
      sh -c '
        exec /serenedb/tests/drivers/run.sh \
          --host serenedb-single \
          --port 5432 \
          --junit /out
      '

networks:
  test-network:
    driver: bridge
    name: ${COMPOSE_PROJECT_NAME}_test-network
