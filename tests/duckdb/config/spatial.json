{
  "description": "SereneDB test-config for the vendored duckdb_spatial test suite. GEOS and PROJ are built; GDAL is not vendored at all (SPATIAL_USE_GDAL=OFF), so every test that reaches a GDAL entry point is expected to fail -- see tests/duckdb/README.md.",
  "skip_tests": [
    {
      "reason": "Geometry zonemap pruning is disabled. spatial's GeometryPredicatePruneCallback reads per-child statistics through FunctionStatisticsPruneInput, using input.function.GetChildren() and input.ChildStats(idx); SereneDB's DuckDB predates that and its FunctionStatisticsPruneInput carries only bind_data and a single stats reference. TryGetGeometryPredicateOperands in src/spatial/util/geometry_predicate_stats.hpp is therefore stubbed to return false, which degrades the callback to NO_PRUNING_POSSIBLE -- correct results, no zonemap elimination. This file asserts that a predicate against a constant outside the column's extent collapses the plan to EMPTY_RESULT; it gets a real scan instead. Restoring it means either forward-porting the richer prune input into the vendored DuckDB or waiting for a DuckDB bump that carries it.",
      "paths": [
        "test/sql/geometry/st_predicate_filter_pushdown.test"
      ]
    },
    {
      "reason": "GDAL is not vendored (SPATIAL_USE_GDAL=OFF in third_party/CMakeLists.txt), so the extension registers none of ST_Read, ST_Read_Meta, ST_Drivers or COPY ... (FORMAT GDAL), and the GDAL /vsi file handler and the replacement scan that rewrites a spatial file name into ST_Read are gone with it. Every file under test/sql/gdal/ reaches one of those entry points. test/sql/shapefile/test_shapefile_read.test converts its fixture through ST_Read before comparing it against ST_ReadSHP; the shapefile reader itself is spatial's own vendored shapelib and still runs (test/sql/gdal/gdal_shapefile.test is the GDAL-side counterpart).",
      "paths": [
        "test/sql/gdal/gdal_axis_order.test",
        "test/sql/gdal/gdal_filter_pushdown.test",
        "test/sql/gdal/gdal_read.test",
        "test/sql/gdal/gdal_shapefile.test",
        "test/sql/gdal/gdal_vsi.test",
        "test/sql/gdal/st_read_gdb.test",
        "test/sql/gdal/st_read_order.test",
        "test/sql/gdal/st_read_xlsx.test",
        "test/sql/gdal/st_write_basic.test",
        "test/sql/gdal/st_write_types.test",
        "test/sql/shapefile/test_shapefile_read.test"
      ]
    },
    {
      "reason": "The persistent RTREE index is not built (SPATIAL_USE_RTREE=OFF). SereneDB indexes geometry through `USING inverted` (iresearch/S2), so spatial's index type, its CREATE INDEX plan, its rtree_index_* pragmas and its scan-pushdown optimizer pass are compiled out rather than merely rejected by the CREATE INDEX access-method whitelist. Every file in test/sql/index/ exercises that index, so the whole directory is skipped. The in-memory FlatRTree that accelerates spatial joins is a separate per-query structure, is still built, and its tests (test/sql/join/) still run.",
      "paths": [
        "test/sql/index/rtree_basic.test",
        "test/sql/index/rtree_basic_data.test_slow",
        "test/sql/index/rtree_basic_points.test",
        "test/sql/index/rtree_block_reclaim.test_slow",
        "test/sql/index/rtree_conflict.test",
        "test/sql/index/rtree_crud.test",
        "test/sql/index/rtree_crud_noreinsert.test",
        "test/sql/index/rtree_dump.test",
        "test/sql/index/rtree_empty.test",
        "test/sql/index/rtree_filter_prune.test",
        "test/sql/index/rtree_filter_pullup.test",
        "test/sql/index/rtree_filter_pullup_2.test",
        "test/sql/index/rtree_insert.test_slow",
        "test/sql/index/rtree_insert_empty.test",
        "test/sql/index/rtree_limits.test",
        "test/sql/index/rtree_multi.test",
        "test/sql/index/rtree_multiple_index.test",
        "test/sql/index/rtree_persistence.test_slow",
        "test/sql/index/rtree_persistence_load.test",
        "test/sql/index/rtree_persistence_wal.test",
        "test/sql/index/rtree_projection.test",
        "test/sql/index/rtree_pushdown.test",
        "test/sql/index/rtree_single.test",
        "test/sql/index/rtree_transaction.test"
      ]
    },
    {
      "reason": "Reads its own output back through a GDAL driver, which is not vendored. st_read_gpkg_fid reads a GeoPackage. The two ST_AsMVT files still exercise the writer -- spatial's MVT module is protozero-based and unaffected -- but verify it by reading the tile back through GDAL's MVT driver; st_asmvtgeom and st_tileenvelope do not read back and still run.",
      "paths": [
        "test/sql/gdal/st_read_gpkg_fid.test",
        "test/sql/mvt/st_asmvt.test",
        "test/sql/mvt/st_asmvt_linestring.test"
      ]
    }
  ]
}
