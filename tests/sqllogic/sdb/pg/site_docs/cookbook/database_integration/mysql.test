control substitution on

# DOCS_TEST: example_003

# DOCS_TEST_BODY

statement error
ATTACH 'host=localhost user=root port=0 database=mysqlscanner' AS mysql_db (TYPE mysql, READ_ONLY);
----
db error: ERROR: Extension "<slt:ignore>" not found.
Extension "mysql" is an existing extension.

Install it first using "INSTALL mysql".
