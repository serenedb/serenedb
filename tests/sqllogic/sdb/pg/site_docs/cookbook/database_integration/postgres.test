# DOCS_TEST: example_001

# DOCS_TEST_BODY

statement error
INSTALL postgres;
----
db error: ERROR: Failed to download extension "postgres_scanner" at URL "https://extensions.duckdb.org/v0.0.1/linux_arm64/postgres_scanner.duckdb_extension.gz"
Extension "postgres_scanner" is an existing extension.

For more info, visit https://duckdb.org/docs/current/extensions/troubleshooting?version=v0.0.1&platform=linux_arm64&extension=postgres_scanner (ERROR Problem with the SSL CA cert (path? access rights?))

# DOCS_TEST: example_002

# DOCS_TEST_BODY

statement error
LOAD postgres;
----
db error: ERROR: Extension "/var/lib/serenedb/.duckdb/extensions/v0.0.1/linux_arm64/postgres_scanner.duckdb_extension" not found.
Extension "postgres" is an existing extension.

Install it first using "INSTALL postgres".

# DOCS_TEST: example_003

# DOCS_TEST_BODY

query error
-- Scan the table "mytable" from the schema "public" in the database "mydb"
SELECT * FROM postgres_scan('host=localhost port=5432 dbname=mydb', 'public', 'mytable');
----
db error: ERROR: Table Function with name "postgres_scan" is not in the catalog, but it exists in the postgres_scanner extension.

This feature relies on the "postgres_scanner" extension which is not built into this SereneDB binary. Please open an issue at https://github.com/serenedb/serenedb/issues if you need it.

# DOCS_TEST: example_004

# DOCS_TEST_BODY

statement error
-- Attach the PostgreSQL database using the given connection string
ATTACH 'host=localhost port=5432 dbname=mydb' AS test (TYPE postgres);
----
db error: ERROR: Extension "/var/lib/serenedb/.duckdb/extensions/v0.0.1/linux_arm64/postgres_scanner.duckdb_extension" not found.
Extension "postgres" is an existing extension.

Install it first using "INSTALL postgres".


query error
-- The table "tbl_name" can now be queried as if it is a regular table
SELECT * FROM test.tbl_name;
----
db error: ERROR: Table with name "test\.tbl_name" does not exist because schema "test" does not exist\.


statement ok
-- Switch the active database to "test"
USE test;

query
-- List all tables in the file
SHOW TABLES;
----
name
