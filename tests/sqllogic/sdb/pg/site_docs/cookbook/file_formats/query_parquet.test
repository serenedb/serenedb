control substitution on

# DOCS_TEST: example_001

statement ok
COPY (SELECT * FROM (VALUES
    (1, 'alpha', 10),
    (2, 'beta', 20)
) AS t("id", "name", "value")) TO '/tmp/${__DATABASE__}_cookbook_file_formats_query_parquet_example_001_input.parquet' (FORMAT parquet);

# DOCS_TEST_RAW
#| SELECT * FROM read_parquet('input.parquet');

# DOCS_TEST_BODY

query
SELECT * FROM read_parquet('/tmp/${__DATABASE__}_cookbook_file_formats_query_parquet_example_001_input.parquet');
----
id	name	value
1	alpha	10
2	beta	20
