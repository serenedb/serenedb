control substitution on

# DOCS_SETUP

statement ok
USE $__DATABASE__;

statement ok
CREATE TABLE tbl (
    id INTEGER,
    name VARCHAR,
    col1 DOUBLE,
    col2 DOUBLE,
    "Some Column Name" VARCHAR,
    i INTEGER,
    a INTEGER,
    b INTEGER,
    c INTEGER,
    number INTEGER,
    number1 INTEGER,
    number2 INTEGER
);

statement ok
INSERT INTO tbl VALUES
    (1, 'mark', 1, 2, 'alpha', 1, 10, 20, 30, 10, 100, 1000),
    (3, 'bookmark', 4, 5, 'beta', 2, 11, 21, 31, 20, 200, 2000),
    (7, 'other', 9, 16, 'gamma', 3, 12, 22, 32, NULL, 300, 3000);

# DOCS_TEST: example_001

statement error db error: ERROR: relation "tbl" already exists
CREATE TABLE tbl (i INTEGER PRIMARY KEY, j VARCHAR);

query error
DESCRIBE tbl;
----
db error: ERROR: unrecognized configuration parameter "tbl"

Did you mean: "block_allocator_memory", "allocator_background_threads", "allocator_bulk_deallocation_flush_threshold", "allocator_flush_threshold", "allow_asterisks_in_http_paths"


query error
SHOW tbl;
----
db error: ERROR: unrecognized configuration parameter "tbl"

Did you mean: "block_allocator_memory", "allocator_background_threads", "allocator_bulk_deallocation_flush_threshold", "allocator_flush_threshold", "allow_asterisks_in_http_paths"


# DOCS_TEST: example_002

statement ok
DESCRIBE SELECT * FROM tbl;

# DOCS_TEST: example_003

statement error
CREATE TABLE tbl_description AS SELECT * FROM (DESCRIBE tbl);
----
db error: ERROR: unrecognized configuration parameter "tbl"

Did you mean: "block_allocator_memory", "allocator_background_threads", "allocator_bulk_deallocation_flush_threshold", "allocator_flush_threshold", "allow_asterisks_in_http_paths"


# DOCS_TEST: example_004

query error db error: ERROR: Problem with the SSL CA cert \(path\? access rights\?\) error for HTTP HEAD to 'https://blobs\.duckdb\.org/data/Star_Trek\-Season_1\.csv'
DESCRIBE TABLE 'https://blobs.duckdb.org/data/Star_Trek-Season_1.csv';
