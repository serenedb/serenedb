control substitution on

# DOCS_TEST: example_003

# DOCS_TEST_RAW
#| CREATE SECRET (
#|     TYPE s3,
#|     KEY_ID '⟨AKIAIOSFODNN7EXAMPLE⟩',
#|     SECRET '⟨wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY⟩',
#|     REGION '⟨us-east-1⟩'
#| );

# DOCS_TEST_BODY

statement ok
CREATE OR REPLACE SECRET export_s3_${__DATABASE__} (
    TYPE s3,
    SCOPE 's3://${MINIO_BUCKET}',
    KEY_ID '${MINIO_ACCESS_KEY}',
    SECRET '${MINIO_SECRET_KEY}',
    ENDPOINT '${MINIO_HOST}:${MINIO_PORT}',
    URL_STYLE 'path',
    USE_SSL false
);

# DOCS_TEST: example_004

# DOCS_TEST_RAW
#| CREATE SECRET (
#|     TYPE s3,
#|     PROVIDER credential_chain
#| );

# DOCS_TEST_BODY

statement ok
CREATE OR REPLACE SECRET export_s3_${__DATABASE__} (
    TYPE s3,
    SCOPE 's3://${MINIO_BUCKET}',
    KEY_ID '${MINIO_ACCESS_KEY}',
    SECRET '${MINIO_SECRET_KEY}',
    ENDPOINT '${MINIO_HOST}:${MINIO_PORT}',
    URL_STYLE 'path',
    USE_SSL false
);

# DOCS_TEST: example_005

# DOCS_TEST_RAW
#| COPY table_name TO 's3://s3-bucket/filename.parquet';

# DOCS_TEST_BODY

statement ok
CREATE TABLE table_name (id INTEGER);

statement ok
COPY table_name TO 's3://${MINIO_BUCKET}/${__DATABASE__}_export_s3.parquet';

# DOCS_TEST: example_006

# DOCS_TEST_RAW
#| CREATE SECRET (
#|     TYPE gcs,
#|     KEY_ID '⟨AKIAIOSFODNN7EXAMPLE⟩',
#|     SECRET '⟨wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY⟩'
#| );

# DOCS_TEST_BODY

statement ok
CREATE OR REPLACE SECRET export_gs_${__DATABASE__} (
    TYPE gcs,
    SCOPE 'gs://${MINIO_BUCKET}',
    KEY_ID '${MINIO_ACCESS_KEY}',
    SECRET '${MINIO_SECRET_KEY}',
    ENDPOINT '${MINIO_HOST}:${MINIO_PORT}',
    URL_STYLE 'path',
    USE_SSL false
);

# DOCS_TEST: example_007

# DOCS_TEST_RAW
#| COPY table_name TO 'gs://gcs_bucket/filename.parquet';

# DOCS_TEST_BODY

statement ok
COPY table_name TO 'gs://${MINIO_BUCKET}/${__DATABASE__}_export_gs.parquet';
