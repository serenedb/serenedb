# DOCS_TEST: example_001

# DOCS_TEST_BODY

query
SELECT ltrim('test', 't');
----
ltrim
est

query
SELECT ltrim('tetris', 'te');
----
ltrim
ris

# DOCS_TEST: example_002

# DOCS_TEST_BODY

query
SELECT rtrim('test', 't');
----
rtrim
tes

query
SELECT rtrim('test...', '.');
----
rtrim
test

# DOCS_TEST: example_003

# DOCS_TEST_BODY

query error
SELECT(split_to_map('a:1;b:2;a:3', ';', ':', (k, v1, v2) -> v1));
----
db error: ERROR: Scalar Function with name split_to_map does not exist!
Did you mean "<slt:ignore>"?


query error
SELECT(split_to_map('a:1;b:2;a:3', ';', ':', (k, v1, v2) -> v2));
----
db error: ERROR: Scalar Function with name split_to_map does not exist!
Did you mean "<slt:ignore>"?

# DOCS_TEST: example_004

# DOCS_TEST_BODY

query error
SELECT strpos('aaa', 'aa', 2);
----
db error: ERROR: No function matches the given name and argument types 'strpos(STRING_LITERAL, STRING_LITERAL, INTEGER_LITERAL)'. You might need to add explicit type casts.
	Candidate functions:
	strpos(VARCHAR, VARCHAR) -> BIGINT

# DOCS_TEST: example_005

# DOCS_TEST_BODY

query error
SELECT strrpos('aaa', 'aa', 2);
----
db error: ERROR: Scalar Function with name strrpos does not exist!
Did you mean "<slt:ignore>"?

# DOCS_TEST: example_006

# DOCS_TEST_BODY

query
SELECT trim('test', 't');
----
btrim
es

query
SELECT trim('.t.e.s.t.', '.t');
----
btrim
e.s
