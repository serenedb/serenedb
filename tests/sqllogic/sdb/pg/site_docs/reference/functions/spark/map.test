# DOCS_TEST: example_001

# DOCS_TEST_BODY

query error
SELECT map(1, 2, 3, 4);
----
db error: ERROR: No function matches the given name and argument types 'map(INTEGER_LITERAL, INTEGER_LITERAL, INTEGER_LITERAL, INTEGER_LITERAL)'. You might need to add explicit type casts.
	Candidate functions:
	map() -> MAP("NULL", "NULL")
	map(K[], V[]) -> MAP(K, V)


query error
SELECT map(1, 2, 3, 4, 1, 5);
----
db error: ERROR: No function matches the given name and argument types 'map(INTEGER_LITERAL, INTEGER_LITERAL, INTEGER_LITERAL, INTEGER_LITERAL, INTEGER_LITERAL, INTEGER_LITERAL)'. You might need to add explicit type casts.
	Candidate functions:
	map() -> MAP("NULL", "NULL")
	map(K[], V[]) -> MAP(K, V)


query error
SELECT map(1, 2, 3, 4, 1, 5);
----
db error: ERROR: No function matches the given name and argument types 'map(INTEGER_LITERAL, INTEGER_LITERAL, INTEGER_LITERAL, INTEGER_LITERAL, INTEGER_LITERAL, INTEGER_LITERAL)'. You might need to add explicit type casts.
	Candidate functions:
	map() -> MAP("NULL", "NULL")
	map(K[], V[]) -> MAP(K, V)


statement error
>
SELECT map(array(1, 2), array(3, 4));
----
db error: ERROR: syntax error at or near ">"

# DOCS_TEST: example_002

# DOCS_TEST_BODY

query error
SELECT map_concat(map(1, 'a', 2, 'b'), map(3, 'c'));
----
db error: ERROR: No function matches the given name and argument types 'map(INTEGER_LITERAL, STRING_LITERAL, INTEGER_LITERAL, STRING_LITERAL)'. You might need to add explicit type casts.
	Candidate functions:
	map() -> MAP("NULL", "NULL")
	map(K[], V[]) -> MAP(K, V)


query error
SELECT map_concat(map(1, 'a', 2, 'b'), map(3, NULL));
----
db error: ERROR: No function matches the given name and argument types 'map(INTEGER_LITERAL, STRING_LITERAL, INTEGER_LITERAL, STRING_LITERAL)'. You might need to add explicit type casts.
	Candidate functions:
	map() -> MAP("NULL", "NULL")
	map(K[], V[]) -> MAP(K, V)


query error
SELECT map_concat(map(1, 'a', 2, 'b'));
----
db error: ERROR: No function matches the given name and argument types 'map(INTEGER_LITERAL, STRING_LITERAL, INTEGER_LITERAL, STRING_LITERAL)'. You might need to add explicit type casts.
	Candidate functions:
	map() -> MAP("NULL", "NULL")
	map(K[], V[]) -> MAP(K, V)


query error
SELECT map_concat(map(1, 'a', 2, 'b'), map(3, 'c', 2, 'd'));
----
db error: ERROR: No function matches the given name and argument types 'map(INTEGER_LITERAL, STRING_LITERAL, INTEGER_LITERAL, STRING_LITERAL)'. You might need to add explicit type casts.
	Candidate functions:
	map() -> MAP("NULL", "NULL")
	map(K[], V[]) -> MAP(K, V)


query error
SELECT map_concat(map(1, 'a', 2, 'b'), map(3, 'c', 2, 'd'));
----
db error: ERROR: No function matches the given name and argument types 'map(INTEGER_LITERAL, STRING_LITERAL, INTEGER_LITERAL, STRING_LITERAL)'. You might need to add explicit type casts.
	Candidate functions:
	map() -> MAP("NULL", "NULL")
	map(K[], V[]) -> MAP(K, V)

# DOCS_TEST: example_003

# DOCS_TEST_BODY

query
SELECT map_entries(MAP(ARRAY[1, 2], ARRAY['x', 'y']));
----
map_entries
<slt:ignore>

# DOCS_TEST: example_004

# DOCS_TEST_BODY

query error
SELECT map_filter(map(1, 0, 2, 2, 3, -1), (k, v) -> k > v);
----
db error: ERROR: Scalar Function with name map_filter does not exist!
Did you mean "<slt:ignore>"?

# DOCS_TEST: example_005

# DOCS_TEST_BODY

query error
SELECT map_from_arrays(array(1.0, 3.0), array('2', '4'));
----
db error: ERROR: syntax error at or near "1\.0"

# DOCS_TEST: example_006

# DOCS_TEST_BODY

query error
SELECT map_zip_with(MAP(ARRAY[1, 2, 3], ARRAY['a', 'b', 'c']), -- \{1 -> ad, 2 -> be, 3 -> cf\}
                    MAP(ARRAY[1, 2, 3], ARRAY['d', 'e', 'f']),
                    (k, v1, v2) -> concat(v1, v2));
----
db error: ERROR: Scalar Function with name map_zip_with does not exist!
Did you mean "<slt:ignore>"?


query error
SELECT map_zip_with(MAP(ARRAY['k1', 'k2'], ARRAY[1, 2]), -- \{k1 -> ROW(1, null), k2 -> ROW(2, 4), k3 -> ROW(null, 9)\}
                    MAP(ARRAY['k2', 'k3'], ARRAY[4, 9]),
                    (k, v1, v2) -> (v1, v2));
----
db error: ERROR: Scalar Function with name map_zip_with does not exist!
Did you mean "<slt:ignore>"?


query error
SELECT map_zip_with(MAP(ARRAY['a', 'b', 'c'], ARRAY[1, 8, 27]), -- \{a -> a1, b -> b4, c -> c9\}
                    MAP(ARRAY['a', 'b', 'c'], ARRAY[1, 2, 3]),
                    (k, v1, v2) -> k || CAST(v1/v2 AS VARCHAR));
----
db error: ERROR: Scalar Function with name map_zip_with does not exist!
Did you mean "<slt:ignore>"?

# DOCS_TEST: example_007

# DOCS_TEST_BODY

query error
SELECT size(map(array(1, 2), array(3, 4)), true);
----
db error: ERROR: syntax error at or near "1"


query error
SELECT size(NULL, true);
----
db error: ERROR: Scalar Function with name size does not exist!
Did you mean "<slt:ignore>"?


query error
SELECT size(NULL, false);
----
db error: ERROR: Scalar Function with name size does not exist!
Did you mean "<slt:ignore>"?
