# DOCS_TEST: example_001

statement ok
CREATE FUNCTION article_url(slug TEXT)
RETURNS TEXT AS $$
    SELECT '/articles/' || slug
$$ LANGUAGE SQL;

query error
SELECT title, article_url(slug) FROM articles;
----
db error: ERROR: Table with name articles does not exist!
Did you mean "<slt:ignore>"?


# DOCS_TEST: example_002

statement error
CREATE FUNCTION top_employees()
RETURNS TABLE(name TEXT, grade INTEGER)
AS $$
    SELECT name, (2 * weight + age) / 10 AS grade
    FROM employees
$$ LANGUAGE SQL;
----
db error: ERROR: Table with name employees does not exist!
Did you mean "<slt:ignore>"?


query error
SELECT * FROM top_employees();
----
db error: ERROR: Table Function with name top_employees does not exist!
Did you mean "<slt:ignore>"?


# DOCS_TEST: example_003

statement error db error: ERROR: syntax error at end of input
CREATE FUNCTION employees_above_grade(grade_threshold INTEGER)
RETURNS TABLE(name TEXT, grade INTEGER)
LANGUAGE SQL
BEGIN ATOMIC
    SELECT name, (2 * weight + age) / 10 AS grade
    FROM employees
    WHERE (2 * weight + age) / 10 > grade_threshold;

statement ok
END;

query error
SELECT * FROM employees_above_grade(16);
----
db error: ERROR: Table Function with name employees_above_grade does not exist!
Did you mean "<slt:ignore>"?


# DOCS_TEST: example_004

query error
SELECT e.name, e.grade, d.department_name
FROM employees_above_grade(10) AS e
JOIN departments d ON e.department_id = d.id;
----
db error: ERROR: Table Function with name employees_above_grade does not exist!
Did you mean "<slt:ignore>"?
