# DOCS_TEST: example_001

# DOCS_TEST_BODY

statement error
CREATE TEXT SEARCH DICTIONARY classifier (
    template = 'classification',
    MODELLOCATION = '/models/cooking.bin',
    TOPK = 3,
    THRESHOLD = 0.5
);
----
db error: ERROR: File "/models/cooking\.bin" referenced by option "modellocation" does not exist
