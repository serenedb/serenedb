control substitution on

# DOCS_TEST: example_001

query
SELECT 1.0 / 0.0 AS x;
----
x
Infinity

query
SELECT 0.0 / 0.0 AS x;
----
x
NaN

query
SELECT -1.0 / 0.0 AS x;
----
x
-Infinity

query
SELECT 'Infinity'::FLOAT / 'Infinity'::FLOAT AS x;
----
x
NaN

query
SELECT 1.0 / 'Infinity'::FLOAT AS x;
----
x
0

query
SELECT 'Infinity'::FLOAT - 'Infinity'::FLOAT AS x;
----
x
NaN

query
SELECT 'Infinity'::FLOAT - 1.0 AS x;
----
x
Infinity

# DOCS_TEST: example_002

query
SELECT 1 / 2 AS x;
----
x
0

# DOCS_TEST: example_003

query
SELECT 1 // 2 AS x;
----
x
0

# DOCS_TEST: example_004

query ? rowsort
SELECT true AS x
UNION
SELECT 2;
----
x
1
2

# DOCS_TEST: example_005

statement ok
CREATE TABLE MyTaBLe (x INTEGER);

query error
SELECT * FROM "MyTaBLe";
----
db error: ERROR: Table with name MyTaBLe does not exist!
Did you mean "<slt:ignore>"?


# DOCS_TEST: example_006

statement ok
CREATE TABLE "PreservedCase" (x INTEGER);

query error
SELECT * FROM PreservedCase;
----
db error: ERROR: Table with name preservedcase does not exist!
Did you mean "<slt:ignore>"?


# DOCS_SETUP

statement ok
USE $__DATABASE__;

statement ok
DROP TABLE IF EXISTS mytable;

# DOCS_TEST: example_007

statement ok
CREATE TABLE MyTaBLe (x INTEGER);

query error
SELECT * FROM "MyTaBLe";
----
db error: ERROR: Table with name MyTaBLe does not exist!
Did you mean "<slt:ignore>"?


statement error db error: ERROR: relation "PreservedCase" already exists
CREATE TABLE "PreservedCase" (x INTEGER);

query error
SELECT * FROM PreservedCase;
----
db error: ERROR: Table with name preservedcase does not exist!
Did you mean "<slt:ignore>"?


query error
SELECT tbl FROM duckdb_tables();
----
db error: ERROR: Referenced column "tbl" not found in FROM clause!
Candidate bindings: "table_oid", "table_name", "database_oid", "database_name", "sql"


# DOCS_SETUP

statement ok
USE $__DATABASE__;

statement ok
DROP TABLE IF EXISTS mytable;

# DOCS_TEST: example_008

statement ok
SET preserve_identifier_case = false;

statement ok
CREATE TABLE MyTaBLe (x INTEGER);

query error
SELECT tbl FROM duckdb_tables();
----
db error: ERROR: Referenced column "tbl" not found in FROM clause!
Candidate bindings: "table_oid", "table_name", "database_oid", "database_name", "sql"


# DOCS_TEST: example_009

query
SELECT 1 == 1 AS t;
----
t
t

# DOCS_TEST: example_010

query
SELECT ARRAY[''''];
----
array
{'}

# DOCS_TEST: example_011

statement ok
CREATE SCHEMA myschema;

statement ok
CREATE TYPE myschema.mytype AS ENUM ('as', 'df');

statement ok
CREATE TABLE myschema.mytable (v mytype);

# DOCS_TEST: example_012

statement ok
DESCRIBE myschema.mytable;

# DOCS_SETUP

statement ok
USE $__DATABASE__;

statement ok
CREATE TABLE tbl (
    id INTEGER,
    name VARCHAR,
    col1 DOUBLE,
    col2 DOUBLE,
    "Some Column Name" VARCHAR,
    i INTEGER,
    a INTEGER,
    b INTEGER,
    c INTEGER,
    number INTEGER,
    number1 INTEGER,
    number2 INTEGER
);

statement ok
INSERT INTO tbl VALUES
    (1, 'mark', 1, 2, 'alpha', 1, 10, 20, 30, 10, 100, 1000),
    (3, 'bookmark', 4, 5, 'beta', 2, 11, 21, 31, 20, 200, 2000),
    (7, 'other', 9, 16, 'gamma', 3, 12, 22, 32, NULL, 300, 3000);

# DOCS_TEST: example_013

statement error db error: ERROR: relation "tbl" already exists
CREATE TABLE tbl (i INTEGER, j INTEGER, PRIMARY KEY (i));

query error
SELECT j
FROM tbl
GROUP BY i;
----
db error: ERROR: Referenced column "j" not found in FROM clause!
Candidate bindings: "a"
