control substitution on

# DOCS_TEST: example_001

# DOCS_TEST_RAW
#| CREATE TEXT SEARCH DICTIONARY classifier AS
#|     classify_text(model_location := '/models/cooking.bin', top_k := 3, threshold := 0.5);

# DOCS_TEST_BODY

statement ok
CREATE TEXT SEARCH DICTIONARY classifier AS
    classify_text(model_location := '${RESOURCES}/tests/iresearch/model_cooking.bin', top_k := 3, threshold := 0.5);

# DOCS_TEST: function_form

# DOCS_TEST_BODY

query
SELECT classify_text('Which baking dish is best to bake a banana bread?', model_location := '${RESOURCES}/tests/iresearch/model_cooking.bin', top_k := 2) AS labels;
----
labels
{__label__bananas,__label__baking}
