# DOCS_TEST: example_024

# DOCS_TEST_BODY

statement ok
CREATE TABLE tbl (col STRUCT(i INTEGER));

statement ok
INSERT INTO tbl VALUES ({'i': 1});

statement ok
ALTER TABLE tbl
ALTER col TYPE STRUCT(i INTEGER, a INTEGER, b VARCHAR)
USING struct_insert(col, a := 42, b := NULL::VARCHAR);

query
SELECT col FROM tbl;
----
col
(1,42,)
