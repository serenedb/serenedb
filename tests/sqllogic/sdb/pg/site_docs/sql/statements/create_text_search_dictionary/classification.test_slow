control substitution on

# DOCS_TEST: example_001

# DOCS_TEST_RAW
#| CREATE TEXT SEARCH DICTIONARY classifier AS
#|     classification(modellocation := '/models/cooking.bin', topk := 3, threshold := 0.5);

# DOCS_TEST_BODY

statement ok
CREATE TEXT SEARCH DICTIONARY classifier AS
    classification(modellocation := '${RESOURCES}/tests/iresearch/model_cooking.bin', topk := 3, threshold := 0.5);
