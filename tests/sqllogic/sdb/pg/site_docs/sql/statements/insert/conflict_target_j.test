# DOCS_SETUP: local_tbl_ijk_fixture

statement ok
CREATE TABLE tbl (i INTEGER PRIMARY KEY, j INTEGER UNIQUE, k INTEGER);

statement ok
INSERT INTO tbl VALUES (1, 20, 300);

# DOCS_TEST: example_025

statement error
INSERT INTO tbl
    VALUES (1, 20, 900)
    ON CONFLICT (j) DO UPDATE SET k = 5 * EXCLUDED.k;
----
db error: ERROR: The specified columns as conflict target are not referenced by a UNIQUE/PRIMARY KEY CONSTRAINT or INDEX
